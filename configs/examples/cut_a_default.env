# Example CLI inputs for the first standalone recipe.
#
# Replace the paths with your actual prepared method inputs.

RECIPE=cut_a_default
ICR_INPUT=/path/to/icr_spans.jsonl
ENTROPY_INPUT=/path/to/entropy_spans.jsonl
DELTA_ENTROPY_INPUT=/path/to/delta_entropy_spans.jsonl
OUTPUT_DIR=/path/to/output
DEVICE=cpu
